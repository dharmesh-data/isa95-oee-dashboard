## Summary

<!-- What does this PR do? -->

## Changes

- 

## Checklist

- [ ] `make lint` passes (`ruff check . && black --check .`)
- [ ] `make test` passes (27/27 unit tests green)
- [ ] Docker images build (`docker build -t test ./simulator && docker build -t test ./flink`)
- [ ] No secrets or credentials committed
